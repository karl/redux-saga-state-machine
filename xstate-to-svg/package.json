{
  "name": "xstate-to-svg",
  "version": "0.10.5",
  "description": "Generate svg image from xstate json",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "git@github.com:karl/redux-saga-state-machine.git",
  "author": "Karl O'Keeffe <github@monket.net>",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build:ci": "npm run lint && npm run build",
    "start": "tsc --watch",
    "test": "jest",
    "lint": "tslint -c ../tslint.json -p tsconfig.json src/**/*.{ts,tsx}",
    "lint:fix": "yarn lint --fix"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/mkdirp": "0.5.2",
    "jest": "23.6.0",
    "jest-cli": "23.6.0",
    "jest-junit": "5.2.0",
    "mkdirp": "0.5.1",
    "prettier": "1.14.3",
    "rimraf": "2.6.2",
    "ts-jest": "23.10.4",
    "xstate": "3.3.3"
  },
  "dependencies": {
    "@types/jest": "^23.3.0",
    "@types/rimraf": "^2.0.2",
    "state-machine-cat": "^4.0.0"
  }
}
