{
  "private": true,
  "workspaces": {
    "packages": [
      "redux-saga-state-machine",
      "xstate-to-svg",
      "example"
    ]
  },
  "scripts": {
    "greenkeeper-lockfile-update": "greenkeeper-lockfile-update",
    "greenkeeper-lockfile-upload": "greenkeeper-lockfile-upload",
    "publish": "lerna publish --skip-npm && git push --follow-tags"
  },
  "devDependencies": {
    "greenkeeper-lockfile": "2",
    "lerna": "^2.11.0"
  }
}
