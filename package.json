{
  "private": true,
  "workspaces": {
    "packages": [
      "redux-saga-state-machine",
      "xstate-to-svg",
      "example"
    ]
  },
  "scripts": {
    "publish": "lerna publish --skip-npm && git push --follow-tags"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
